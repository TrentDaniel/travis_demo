import pytest

def test_wheel_install():
#    x = None
#    with pytest.raises(ZeroDivisionError) as exception:
#        x = 1 / 0
#    assert "division by zero" in str(exception.value), "Did not detect the exception properly"
#    assert x == 5, "This should not be reached"
#    with pytest.raises(ZeroDivisionError):
#    try:
#        import wheel
    import wheel
